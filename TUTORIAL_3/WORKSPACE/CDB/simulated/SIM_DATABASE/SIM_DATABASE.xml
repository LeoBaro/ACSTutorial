<SimulatedComponent xmlns="urn:schemas-cosylab-com:SimulatedComponent:1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <pythonImports>
from acstime import Duration
import random
    </pythonImports>
    <initialize>
proposal_ids = [] 
logger=parameters[0].getLogger()
logger.logInfo("================> Simulated Database Component initialized!")
    </initialize>
    <cleanUp>
print('Simulated component cleaning up')
    </cleanUp>
    <_corbaMethod Name="storeProposal" Timeout="0">
print("================> storeProposal")
proposal_ids = [random.randint(0,10) for i in range(5)]
proposal_ids.sort()
proposal_ids = list(set(proposal_ids))
if params.pid not in proposal_ids:
    print("\nStoring proposal ", parameters, "\n")
    print("\nCurrent proposals: ", proposal_ids, "\n")
    return True
else:
    return False
    </_corbaMethod>
    <_corbaMethod Name="countProposals" Timeout="0">
print("================> countProposals")
return random.randint(0,10)
    </_corbaMethod>
</SimulatedComponent>